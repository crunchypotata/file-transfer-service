<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-file="http://www.springframework.org/schema/integration/file"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/integration
           https://www.springframework.org/schema/integration/spring-integration.xsd
           http://www.springframework.org/schema/integration/file
           https://www.springframework.org/schema/integration/file/spring-integration-file.xsd">

    <int-file:inbound-channel-adapter id="filesIn"
                                      directory="source">
        <int:poller fixed-delay="1000"/>
    </int-file:inbound-channel-adapter>

    <int:transformer input-channel="filesIn"
                     output-channel="toLoggingChannel"
                     ref="fileProcessingService"
                     method="renameAndEnrich"/>

    <int:publish-subscribe-channel id="toLoggingChannel"/>

    <int:logging-channel-adapter id="xmlAuditLogger"
                                 channel="toLoggingChannel"
                                 level="INFO"
                                 expression="'XML AUDIT | File: ' + headers.originalFileName + ' | Size: ' + headers.fileSize"/>

    <int:recipient-list-router input-channel="toLoggingChannel">
        <int:recipient selector-expression="payload.name.toLowerCase().endsWith('.txt')" channel="txtOut"/>
        <int:recipient selector-expression="!payload.name.toLowerCase().endsWith('.txt')" channel="otherOut"/>
    </int:recipient-list-router>

    <int-file:outbound-channel-adapter id="txtOut"
                                       directory="target_txt"
                                       delete-source-files="true"/>

    <int-file:outbound-channel-adapter id="otherOut"
                                       directory="target_other"
                                       delete-source-files="true"/>

</beans>